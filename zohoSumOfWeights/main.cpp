#include <iostream>
#include <map>

using namespace std;

int main()
{
    map<int,int> m1;
    int numbers[] = {10, 36, 54, 89, 12};

    for(int index=0; index<(sizeof(numbers)/sizeof(numbers[0]); index++))
    {
        int value;
        if(numbers[i] == )
        m1.insert(make_pair(numbers[i],0));
    }

    return 0;
}
